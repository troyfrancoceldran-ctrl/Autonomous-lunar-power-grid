"""
Entry point. Builds a scenario (environment + assets + controller), runs
SimulationEngine, and triggers metrics/visualization.

STATUS: not runnable yet — wires together modules that don't exist until
Step 9 (simulation_engine.py) is done. See PROGRESS.md.
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "main.py will be wired up once simulation_engine.py exists (Step 9)."
    )
